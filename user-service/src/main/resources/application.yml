spring:
  datasource:
    url:  jdbc:h2:C:/data/userdb
    driver-class-name:  org.h2.Driver
    username: sailendra131
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-ui

server:
  port: 8081